<%= form_with(model: @book, local: false) do |form| %>
   <div class="field">
    <%= form.label :title,'What is Book are you buy?' %>
    <div class="field">

    <select class="form-select mt-1 mb-2" name="book_id" id="category_select" aria-label="Default select example">
      <%= @reserve.each do |a| %>
      <option value="<%= a.id %>"><%= a.book.title %></option>
      <% end %>
    </select>
    <%= form.hidden_field :user_id, value: current_user.id%>
  
  <div class="actions">
    <%= form.submit "To Pay", class:'btn btn-success' %>
  </div>
<% end %>
 
 <script>
 setTimeout(function(){ 
   alert("Finish time for buying"); 
   $('#form').html(' ')
 }, 3000);

 
 </script>